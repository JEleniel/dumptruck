[package]
name = "simple_enricher"
version = "0.1.0"
edition = "2021"

[lib]
name = "simple_enricher"
path = "src/lib.rs"

[dependencies]
# This is a standalone example. To integrate with the main crate, add a
# path dependency on the root crate and implement `crate::enrichment::EnrichmentPlugin`.
